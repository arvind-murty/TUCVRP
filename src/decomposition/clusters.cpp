#include "tucvrp/decomposition.hpp"

namespace tucvrp {

void DecompositionBuilder::decompose_blocks_into_clusters(TreeDecomposition& decomposition,
                                                          const RootedTreeData& rooted_tree,
                                                          double epsilon) {
    (void)decomposition;
    (void)rooted_tree;
    (void)epsilon;
    // TODO(section 4.2): split each block into clusters.
}

}  // namespace tucvrp
